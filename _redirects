# Netlify _redirects — pretty URLs.
# https://docs.netlify.com/routing/redirects/
#
# Netlify's "Pretty URLs" feature is on by default for deploys, but we
# add explicit 200-rewrites for the canonical short paths the
# security.txt + Responsible Disclosure documents reference. Explicit
# rules guard against a future Netlify config change that flips Pretty
# URLs off.

/security        /security.html         200
/status          /status.html           200
/docs            /docs.html             200
/cctp-explainer  /cctp-explainer.html   200
/demo            /demo.html             200
/pricing         /pricing.html          200

# Canonical host. Keep the public surface on the bare domain so every
# canonical, sitemap, Open Graph URL, and AI-readable file points at one
# entity.
https://www.chainmore.io/*  https://chainmore.io/:splat  301!

# A previously exposed non-public API spec was pulled from the public
# surface 2026-05-12. Anyone hitting the old public path (cached
# bookmark, old marketing copy, scraper) gets a 410 Gone so they don't
# treat it as "temporarily missing".
#
# Netlify _redirects requires a destination URL even for status-only
# responses. Send to /404.html with explicit 410 status — the body
# users see is the branded 404 page, but the response status is the
# correct "gone permanently".
/api/treasury.yaml  /404.html  410!
